import React from 'react';

// This component is now removed as the ambient sound feature wasn't working properly
// We'll add love-themed animations instead throughout the site

const AmbientSound: React.FC = () => {
  return null;
};

export default AmbientSound;